<h1>Burgers!</h1>

<h2>Choose from any of our yummy burgers on the Menu!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{name}}

      <button class="eat-burger" data-id="{{this.id}}">Eat the burger!</button>

    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you've eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{name}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger to our Secret Menu</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burgerName" name="name">
  </div>

  <div class="form-group">
    <label for="devoured"> Have you eaten this burger?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Not devoured
  </div>

  <button type="submit">Add Burger</button>
</form>
